#include "motor.h"
#include "setup.h"
#include "Arduino.h"
// set 
void motors_setup() {
  // pinMode(ENCA,INPUT); // sets pin2 as the input
  // pinMode(ENCB,INPUT); // sets pin3 as the input
  // pinMode(DC_MOTOR_1A, OUTPUT);
  // pinMode(DC_MOTOR_1B, OUTPUT);
  // pinMode(DC_MOTOR_2A, OUTPUT);
  // pinMode(DC_MOTOR_2B, OUTPUT);
}


// void move_forward() {
//     digitalWrite(DC_MOTOR_1A, HIGH);
//     digitalWrite(DC_MOTOR_1B, LOW);
//     digitalWrite(DC_MOTOR_2A, HIGH);
//     digitalWrite(DC_MOTOR_2B, LOW);
// }